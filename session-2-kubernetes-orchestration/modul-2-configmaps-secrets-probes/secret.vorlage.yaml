apiVersion: v1
kind: Secret
metadata:
  name: api-secret
type: Opaque
data:
  # TODO: Wert per `echo -n "<token>" | base64` erzeugen und hier eintragen,
  # anschließend diese Datei als secret.yaml speichern (nicht committen!).
  API_TOKEN: __TODO_BASE64_TOKEN__
